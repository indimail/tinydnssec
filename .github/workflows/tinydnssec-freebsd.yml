name: tinydnssec FreeBSD CI

on:
  push:
    branches:
      - master
    paths:
      - '**/tinydnssec-1.05/**'
      - .github/workflows/tinydnssec-c-cpp.yml
      - '!**/tinydnssec-1.05/tinydnssec.spec.in'
      - '!**/tinydnssec-1.05/debian/*'
      - '!**/tinydnssec-1.05x/doc/*'
      - '!**.md'
  pull_request:
    branches: [ master ]

jobs:
  testfreebsd:
    runs-on: macos-latest
    name: FreeBSD
    env:
      MYTOKEN : ${{ secrets.MYTOKEN }}
      MYTOKEN2: "value2"
    steps:
    - uses: actions/checkout@v2
      with:
        path: main
    - uses: actions/checkout@v2
      with:
        repository: mbhangui/libqmail
        path: libqmail
    - name: build tinydnssec
      id: FreeBSD
      uses: vmactions/freebsd-vm@v0.1.2
      with:
        envs: 'MYTOKEN MYTOKEN2'
        usesh: true
        prepare: pkg install -y curl automake autoconf libtool pkgconf
        run: |
          cd libqmail; ./default.configure; make; make install-strip
          cd ../main/tinydnssec-1.05; ./default.configure; make
          cd dq-20161210; make
          cd ../curvedns-0.88; make
