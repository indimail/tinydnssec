#!/bin/sh
set -x
make clean
make distclean
ver=1.05
cd ..
if [ -f $HOME/.rpmmacros ] ; then
	topdir=`grep ^%_topdir $HOME/.rpmmacros|awk '{print $2}'`
else
	topdir=$HOME/rpmbuild
fi
tar cf - tinydnssec-$ver | gzip -c > $topdir/SOURCES/tinydnssec-$ver.tar.gz
cd tinydnssec-$ver
rpmbuild -ba --clean tinydnssec.spec
